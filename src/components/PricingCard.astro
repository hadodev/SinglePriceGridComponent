<div class="card-container">
  <article>
    <section>
      <h2>Join our community</h2>
      <h3>30-day, hassle-free money back guarantee</h3>
      <p>
        Gain access to our full library of tutorials along with expert code reviews. Perfect for any developers who are
        serious about honing their skills.
      </p>
    </section>
    <section>
      <h2>Monthly Subscription</h2>
      <p>&dollar;29 <span>per month</span></p>
      <p>Full access for less than &dollar;1 a day</p>
      <a href="/">Sign Up</a>
    </section>
    <section>
      <h2>Why Us</h2>
      <ul>
        <li>Tutorials by industry experts</li>
        <li>Peer &amp; expert code review</li>
        <li>Coding exercises</li>
        <li>Access to our GitHub repos</li>
        <li>Community forum</li>
        <li>Flashcard decks</li>
        <li>New videos every week</li>
      </ul>
    </section>
  </article>
</div>

<style>
  .card-container {
    container-type: inline-size;

    article {
      --shadow: rgba(149, 157, 165, 0.2) 0px 8px 24px;

      display: grid;

      @container (min-width: calc((25ch + 3em) * 2)) {
        grid-template-columns: repeat(2, 1fr);
      }

      color: var(--clr-neutral-light-gray);
      border-radius: 0.5rem;
      box-shadow: var(--shadow);
    }

    h2 {
      font-weight: 700;
      font-size: 1.2rem;
      margin-block-end: 1em;
      margin-block-start: 0;
    }

    section {
      padding-block: clamp(1.5em, 1.3em + 2vw, 2.5em);
      padding-inline: clamp(1.5em, 1.3em + 3vw, 3em);
    }

    section:nth-child(1) {
      grid-column: 1 / -1;
      background-color: white;
      border-top-left-radius: inherit;
      border-top-right-radius: inherit;

      h2 {
        color: var(--clr-accent-cyan);
      }

      h3 {
        color: var(--clr-accent-bright-yellow);
        margin-block-end: 1rem;
        font-weight: 700;
      }

      p {
        max-width: 60ch;
        color: var(--clr-neutral-grayish-blue);
        line-height: 1.8;
      }
    }

    section:nth-child(2) {
      display: flex;
      flex-direction: column;
      background-color: var(--clr-accent-cyan);
      /* min-height: 15rem; */

      @container (min-width: calc((25ch + 3em) * 2)) {
        border-bottom-left-radius: inherit;
      }

      p {
        display: flex;
        align-items: center;
        gap: 1rem;
        line-height: 1.1;

        &:first-of-type {
          font-weight: 700;
          font-size: 2.5rem;
        }

        &:last-of-type {
          margin-block-end: 1.5em;
        }
      }

      span {
        font-size: initial;
        font-weight: initial;
        opacity: 0.8;
      }

      a {
        display: block;
        width: 100%;
        text-align: center;
        padding-block: 1em;
        background-color: var(--clr-accent-bright-yellow);
        margin-top: auto;
        border-radius: 0.3rem;
        font-weight: 700;
        box-shadow: var(--shadow);
        text-decoration: none;
        color: inherit;
      }
    }

    section:nth-child(3) {
      background-color: hsl(from var(--clr-accent-cyan) h s l / 0.8);
      border-bottom-right-radius: inherit;
      border-bottom-left-radius: inherit;

      @container (min-width: calc((25ch + 3em) * 2)) {
        border-bottom-left-radius: 0;
      }

      ul {
        list-style: none;
        padding: 0;
        margin: 0;
        line-height: 1.3;
        opacity: 0.8;

        li {
          color: inherit;
        }
      }
    }
  }
</style>
